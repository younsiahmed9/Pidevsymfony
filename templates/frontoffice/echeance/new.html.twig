{% extends 'frontoffice/base.html.twig' %}

{% block title %}Nouvelle Échéance - FinTrack{% endblock %}

{% block body %}
<section class="py-5 bg-light min-vh-100">
    <div class="container py-4">
        <div class="card border-0 shadow-sm rounded-4">
            <div class="card-header bg-white d-flex justify-content-between align-items-center">
                <h1 class="h5 mb-0">Nouvelle échéance</h1>
                <a href="{{ path('echeance_index') }}" class="btn btn-outline-secondary btn-sm">Retour</a>
            </div>
            <div class="card-body p-4">
                <form method="post" class="needs-validation echeance-form" novalidate data-validate-url="{{ path('echeance_validate') }}">
                    {% include 'frontoffice/echeance/_form.html.twig' %}
                    <div class="mt-4 d-flex gap-2">
                        <button class="btn btn-primary">Créer</button>
                        <a href="{{ path('echeance_index') }}" class="btn btn-outline-secondary">Annuler</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>
{% endblock %}
