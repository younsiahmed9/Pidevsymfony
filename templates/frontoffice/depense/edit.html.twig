{% extends 'frontoffice/base.html.twig' %}

{% block title %}Modifier la dépense - FinTrack{% endblock %}

{% block body %}
<section class="py-5 bg-light min-vh-100">
    <div class="container py-4">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <!-- Breadcrumb -->
                <nav aria-label="breadcrumb" class="mb-4">
                    <ol class="breadcrumb bg-transparent p-0 small text-uppercase fw-bold">
                        <li class="breadcrumb-item"><a href="{{ path('depense_index') }}" class="text-decoration-none text-muted">Mes Dépenses</a></li>
                        <li class="breadcrumb-item active text-primary" aria-current="page">Modifier la dépense</li>
                    </ol>
                </nav>

                <!-- Card -->
                <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
                    <div class="card-header bg-warning text-white py-4 px-5">
                        <div class="d-flex align-items-center justify-content-between">
                            <div class="d-flex align-items-center">
                                <div class="bg-white bg-opacity-25 rounded-circle p-3 me-4">
                                    <i class="fas fa-edit fa-2x"></i>
                                </div>
                                <div>
                                    <h2 class="h3 mb-1 fw-bold">Modifier la dépense</h2>
                                    <p class="mb-0 text-white-50">Transaction #{{ depense.id_depense|default('') }} - {{ depense.categorie|default('') }}</p>
                                </div>
                            </div>
                            {{ include('frontoffice/depense/_delete_form.html.twig') }}
                        </div>
                    </div>
                    <div class="card-body p-5 bg-white">
                        {{ include('frontoffice/depense/_form.html.twig', {'button_label': 'Mettre à jour la dépense'}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
