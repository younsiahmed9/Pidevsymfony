<form method="post" action="{{ action }}" onsubmit="return confirm('Supprimer ce dossier ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier.id) }}">
    <button class="dropdown-item py-2 text-danger" type="submit"><i class="fas fa-trash-alt me-2 small"></i> Supprimer</button>
</form><form method="post" action="{{ path('dossier_document_delete', {'id': dossier.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce dossier ?');" style="display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier.id) }}">
    <button class="btn btn-outline-danger btn-sm rounded-pill fw-bold shadow-sm px-4">
        <i class="fas fa-trash-alt me-2"></i> Supprimer ce dossier
    </button>
</form>